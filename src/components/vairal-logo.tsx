export function VairalLogo({ className = "h-28" }: { className?: string }) {
  return (
    <div className={`${className} flex items-center text-4xl font-black tracking-tighter bg-gradient-to-r from-blue-500 to-indigo-500 bg-clip-text text-transparent`}>
      VAIRAL
    </div>
  );
}
